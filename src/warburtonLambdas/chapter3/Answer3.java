package warburtonLambdas.chapter3;

public class Answer3 {
//    public void getAnswer(List<Artist> artists) {
//        int sum = artists.stream()
////                .map(artist -> artist.getMembers())
//                .reduce(0, (artist.getMembers(), element) -> acc + element);
//    }

//    public void getAnswer2(List<Artist> artists) {
//        int sum = artists.stream()
//                .count(Comparator.comparing(artist -> artist.getMembers().size()))
//                .get();
//    }

    // Q3
//    public static int countBandMembersInternal(List<Artist> artists) {
//        // NB: readers haven't learnt about primitives yet, so can't use the sum() method
//        return artists.stream()
//                .map(artist -> artist.getMembers().count())
//                .reduce(0, Integer::sum)
//                .intValue();
//    }
}